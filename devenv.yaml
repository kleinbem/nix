# devenv.yaml
# Minimal configuration to enable devenv CLI features
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
  git-hooks:
    url: github:cachix/git-hooks.nix
  nixos-generators:
    url: github:nix-community/nixos-generators
