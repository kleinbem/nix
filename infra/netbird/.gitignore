# Terraform / OpenTofu — state lives in R2, keep local artifacts out.
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
.r2-endpoint.hcl
crash.log

# First-apply adoption ids (tenant-specific; generated by `just ids`).
imports.auto.tfvars
