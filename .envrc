#!/usr/bin/env bash
export NIXPKGS_ALLOW_UNFREE=1
# Load the workspace shell from nix-devshells (no meta flake.nix here anymore).
# nix-devshells is a flat sibling repo, not nested under nix/ — see repos.nix.
use flake ../nix-devshells#workspace
