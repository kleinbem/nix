name: "Build & Cache Devshells"

on:
  push:
    branches:
      - main
      - master
    paths:
      - ".github/workflows/build-devshells.yaml"
      - "flake.nix"
      - "flake.lock"
      - "devenv.nix"
      - "nix-devshells/**"
  pull_request:
    paths:
      - ".github/workflows/build-devshells.yaml"
      - "flake.nix"
      - "flake.lock"
      - "devenv.nix"
      - "nix-devshells/**"
  workflow_dispatch:

jobs:
  # ---------------------------------------------------------
  # STAGE 1: Discover all exposed devShells
  # ---------------------------------------------------------
  generate-matrix:
    name: "Discover Devshell Matrix"
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Init submodules (skip nix-secrets)
        run: |
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          git -c submodule."nix-secrets".update=none submodule update --init --recursive

      - name: Set up dummy secrets
        run: ./scripts/write-dummy-secrets.sh

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Set Nix input overrides
        run: |
          echo "NIX_OVERRIDES=\
            --override-input nix-secrets /tmp/dummy-secrets \
            --override-input nix-config git+file://$GITHUB_WORKSPACE/nix-config \
            --override-input nix-devshells git+file://$GITHUB_WORKSPACE/nix-devshells \
            --override-input nix-presets git+file://$GITHUB_WORKSPACE/nix-presets \
            --override-input nix-hardware git+file://$GITHUB_WORKSPACE/nix-hardware \
            --override-input nix-packages git+file://$GITHUB_WORKSPACE/nix-packages \
            --override-input nix-templates git+file://$GITHUB_WORKSPACE/nix-templates" >> "$GITHUB_ENV"

      - name: Generate Dynamic Matrix
        id: set-matrix
        run: |
          MATRIX_JSON=$(nix eval .#devShells.x86_64-linux --impure --apply 'builtins.attrNames' --json $NIX_OVERRIDES)
          echo "Discovered Devshells: $MATRIX_JSON"
          echo "matrix=$MATRIX_JSON" >> "$GITHUB_OUTPUT"

  # ---------------------------------------------------------
  # STAGE 2: Build & cache each devshell closure
  # ---------------------------------------------------------
  build-devshell:
    name: "Build devShell ${{ matrix.shell }}"
    needs: generate-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      # Serialize so devshells don't push to Attic concurrently (shared chunks race
      # → "Bad chunk hash or size").
      max-parallel: 1
      matrix:
        shell: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 512
          swap-size-mb: 1024
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-docker-images: 'true'
          remove-codeql: 'true'

      - name: Checkout repository
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Init submodules (skip nix-secrets)
        run: |
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          git -c submodule."nix-secrets".update=none submodule update --init --recursive

      - name: Set up dummy secrets
        run: ./scripts/write-dummy-secrets.sh

      - name: Set Nix input overrides
        run: |
          echo "NIX_OVERRIDES=\
            --override-input nix-secrets /tmp/dummy-secrets \
            --override-input nix-config git+file://$GITHUB_WORKSPACE/nix-config \
            --override-input nix-devshells git+file://$GITHUB_WORKSPACE/nix-devshells \
            --override-input nix-presets git+file://$GITHUB_WORKSPACE/nix-presets \
            --override-input nix-hardware git+file://$GITHUB_WORKSPACE/nix-hardware \
            --override-input nix-packages git+file://$GITHUB_WORKSPACE/nix-packages \
            --override-input nix-templates git+file://$GITHUB_WORKSPACE/nix-templates" >> "$GITHUB_ENV"

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22
        with:
          extra-conf: |
            sandbox = false
            trusted-users = root runner

      - name: Setup Attic Cache
        uses: ryanccn/attic-action@v0
        with:
          endpoint: https://cache.kleinbem.dev/
          cache: system
          token: ${{ secrets.ATTIC_PUSH_TOKEN }}

      - name: Build devShell closure
        run: |
          nix build ".#devShells.x86_64-linux.${{ matrix.shell }}" \
            --impure -L $NIX_OVERRIDES

      - name: Push to Attic
        run: |
          attic push system $(nix-store -qR result)
